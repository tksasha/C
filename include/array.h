#define  ASC 0
#define DESC 1

int array_max(int *array, size_t size);

void array_sort(int *array, size_t size, short mode);

int array_shift(int *array, size_t size);

void array_unshift(int *array, int el, size_t size);
